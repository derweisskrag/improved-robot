// get buttons that redirect the user
const pushToLoginBtn = document.querySelector(".push-to-login");
const pushToSignUpBtn = document.querySelector(".push-to-signup");

// get buttons that submit the form
const submitLoginBtn = document.querySelector(".submit-login");
const submitSingUpBtn = document.querySelector(".submit-signup");

// TODO: Implement navigation across links

// get the links from the menu
const links = document.querySelectorAll(".menu");

// implement active links